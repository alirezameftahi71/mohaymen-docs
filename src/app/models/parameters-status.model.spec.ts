import { ParametersStatus } from './parameters-status.model';

describe('ParametersStatus', () => {
  it('should create an instance', () => {
    expect(new ParametersStatus()).toBeTruthy();
  });
});
